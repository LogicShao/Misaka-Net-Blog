# Misaka Blog Admin - 桌面管理应用

⚡ 基于 Electron 的 Astro 博客桌面管理工具

## 功能特性

✨ **核心功能**
- 📝 创建、编辑、删除博客文章
- 🏷️ 管理文章元数据（标题、描述、标签、日期）
- 📋 实时文章列表预览
- 🔍 搜索文章（支持标题和标签搜索）
- 📊 文章统计（总数、已发布、草稿）
- 🔨 一键构建博客
- 💾 自动保存编辑内容

✨ **用户体验**
- 🎨 现代化 UI 设计
- ⚡ 快捷键支持（Ctrl+N 新建、Ctrl+B 构建、Ctrl+R 刷新）
- 🌓 舒适的编辑器界面
- 📱 响应式布局
- 🔔 状态提示和反馈

## 安装和使用

### 开发模式运行

```bash
# 1. 进入应用目录
cd electron-admin

# 2. 启动应用
npm start

# 或使用调试模式
npm run dev
```

### 打包应用

```bash
# 打包 Windows 应用
npm run build:win

# 打包 macOS 应用
npm run build:mac

# 打包 Linux 应用
npm run build:linux

# 打包所有平台
npm run build:all
```

打包后的应用会生成在 `electron-admin/dist` 目录下。

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+N` / `Cmd+N` | 新建文章 |
| `Ctrl+B` / `Cmd+B` | 构建博客 |
| `Ctrl+R` / `Cmd+R` | 刷新列表 |
| `Ctrl+S` / `Cmd+S` | 保存文章（编辑时）|
| `F11` | 全屏切换 |
| `F12` | 开发者工具 |
| `Ctrl+Q` / `Cmd+Q` | 退出应用 |

## 界面说明

### 主界面布局

```
┌─────────────────────────────────────────────┐
│  ⚡ Misaka Blog Admin        [➕新建 🔨构建 🔄刷新] │
├─────────────────────────────────────────────┤
│                                             │
│  📚 文章列表    │    ✏️ 编辑器               │
│  ┌──────────┐  │    ┌──────────────────┐    │
│  │  搜索框   │  │    │  [元数据|内容|预览] │    │
│  ├──────────┤  │    ├──────────────────┤    │
│  │统计信息   │  │    │                  │    │
│  ├──────────┤  │    │  编辑区域         │    │
│  │文章1 ⬅️  │  │    │                  │    │
│  │文章2     │  │    │                  │    │
│  │文章3     │  │    │                  │    │
│  │  ...     │  │    └──────────────────┘    │
│  └──────────┘  │    [💾保存 🗑️删除 ❌取消]    │
│                                             │
└─────────────────────────────────────────────┘
```

### 编辑器标签

1. **元数据**：编辑文章的 frontmatter（标题、描述、日期、标签、草稿状态）
2. **内容**：编辑 Markdown 内容
3. **预览**：查看原始数据

## 文章文件格式

应用自动处理 Markdown 文件的 frontmatter 格式：

```markdown
---
title: '文章标题'
description: '文章描述'
pubDate: 2025-11-22
tags: ['标签1', '标签2']
draft: false
---

文章内容（Markdown 格式）...
```

## 技术栈

- **Electron** 28.0.0 - 跨平台桌面应用框架
- **Node.js** - 后端运行时
- **Pure HTML/CSS/JS** - 前端界面（无框架依赖，轻量高效）

## 目录结构

```
electron-admin/
├── main.js              # Electron 主进程
├── preload.js           # 预加载脚本（IPC 桥接）
├── package.json         # 项目配置
├── renderer/            # 渲染进程（前端）
│   ├── index.html       # 主界面
│   ├── styles.css       # 样式文件
│   └── renderer.js      # 前端逻辑
└── dist/                # 打包输出目录
```

## 与旧版对比

| 特性 | 旧版 (admin-server.js) | 新版 (Electron) |
|------|----------------------|----------------|
| 运行方式 | 浏览器访问 localhost | 桌面应用 |
| 依赖 | 需要手动启动服务器 | 双击即可运行 |
| 性能 | HTTP 请求 | IPC 通信（更快） |
| 用户体验 | 需要浏览器 | 原生桌面应用 |
| 快捷键 | 不支持 | 支持 |
| 菜单栏 | 无 | 完整菜单系统 |
| 打包部署 | 需要 Node 环境 | 独立可执行文件 |

## 故障排除

### 应用无法启动

1. 确保 Node.js 版本 >= 18
2. 删除 `node_modules` 后重新运行 `npm install`
3. 检查 `main.js` 中的 `BLOG_ROOT` 路径是否正确

### 找不到文章

确认博客文章位于正确的目录：`../src/content/blog/`

### 构建失败

1. 确保博客根目录的 `package.json` 中有 `build` 脚本
2. 检查 `buildLog` 中的错误信息
3. 手动运行 `npm run build` 测试构建流程

## 开发说明

### 修改应用逻辑

- 主进程逻辑：编辑 `main.js`
- 前端界面：编辑 `renderer/index.html`
- 前端样式：编辑 `renderer/styles.css`
- 前端逻辑：编辑 `renderer/renderer.js`
- IPC 通信：编辑 `preload.js`

### 调试技巧

1. 使用 `npm run dev` 启动，自动打开开发者工具
2. 在 `main.js` 中使用 `console.log()` 查看主进程日志
3. 在浏览器开发者工具中查看渲染进程日志

## 许可证

MIT License

© 2025 Misaka Network

---

**注意：** 本应用需要配合 Astro 博客项目使用，确保将 `electron-admin` 目录放置在博客项目的根目录下。
